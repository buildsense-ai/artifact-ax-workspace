{
  "name": "@artifact-ax/trigger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Transport-neutral intelligent trigger, Agent Bridge, AG-UI projection, and Cloud Artifact Host adapters. Selections are context, not a command.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  }
}
