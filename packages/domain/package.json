{
  "name": "@artifact-ax/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deep in-memory Artifact domain service: describe/inspect/apply/watch/publish with Draft/Published versions, optimistic revisions, idempotency, approvals, and audit events.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@artifact-ax/contract": "workspace:*"
  }
}