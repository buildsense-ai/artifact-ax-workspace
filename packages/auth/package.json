{
  "name": "@artifact-ax/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Transport-neutral identity boundary for Artifact AX: CatsCo introspection, opaque sessions, and explicit scopes.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  }
}
