{
  "name": "@artifact-ax/lesson-report",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Example structured Artifact: a teaching report with a review table, summary panel, approval panel, and an approval-gated capability used by the demo SPA and the artifact node.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@artifact-ax/contract": "workspace:*",
    "@artifact-ax/domain": "workspace:*"
  }
}