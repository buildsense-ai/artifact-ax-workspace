{
  "name": "@artifact-ax/contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared AX contract: manifest, commands, events, revisions, approvals, and CatsCo cloud-artifacts index/management types with runtime validation.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  }
}